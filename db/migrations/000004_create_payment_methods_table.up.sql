CREATE TABLE payment_methods (
                                 id SERIAL PRIMARY KEY,
                                 logo VARCHAR(255),
                                 name VARCHAR(255) NOT NULL
);